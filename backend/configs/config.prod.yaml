api:
  host: "0.0.0.0"
  port: 10001
admin:
  host: "0.0.0.0"
  port: 10002
mysql:
  data_source: "admin:6HNzatYAu]+G@tcp(database-1.cdeiw4yyml4x.ap-southeast-1.rds.amazonaws.com:3306)/octo_prod?timeout=60s&readTimeout=180s&writeTimeout=180s&charset=utf8mb4&parseTime=true"
  max_idle_conns: 10
  max_open_conns: 100
  max_lifetime: 3600
  max_idletime: 1800
log:
  level: "debug"
  encoding: "json"
  api_output_path: "logs/api.log"
  admin_output_path: "logs/admin.log"
  max_size: 100
  max_backups: 30
  max_age: 7
  compress: true
email:
  provider: "resend"
  local:
    expose_code: false
  resend:
    # TODO: Do not commit real API keys. Set RESEND_API_KEY in backend/configs/.env
    # or deployment environment variables before running production.
    api_key: ""
    # TODO: Replace with a verified Resend sender domain/address before production launch.
    from_email: "Octo Agent <no-reply@mail.octo-agent.com>"
  ses:
    region: "ap-southeast-1"
    # TODO: SES is kept only as a compatibility provider. Do not commit real AWS keys.
    access_key_id: ""
    secret_access_key: ""
    from_email: "no-reply@mail.octo-agent.com"

app:
  frontend_base_url: "https://your-frontend.example.com"

x_oauth:
  client_id: ""
  client_secret: ""
  redirect_uri: "https://your-api.example.com/api/v1/accounts/oauth/x/callback"
  state_secret: ""

billing:
  order_ttl_minutes: 30
  webhook_secret: ""
  rpc_urls:
    "56": "https://go.getblock.io/37bd62f02647427693e166fbad5cc53c"
  payment_methods:
    - method: "USDT"
      network: "BEP20"
      chain_id: 56
      token_address: "0x0000000000000000000000000000000000000000"
      receiver_address: "0x0000000000000000000000000000000000000000"
      decimals: 18
      is_default: true
      note: "Replace token_address and receiver_address with production USDT treasury."
  plans:
    basic_monthly:
      name: "Basic"
      amount: "10"
      currency: "USDT"
      period_days: 30
      description: "Full automation for one month."
