# Optional backend environment overrides.
# Copy to backend/configs/.env for local development.

APP_ENV=local

# Email provider overrides.
# EMAIL_PROVIDER=local
# RESEND_API_KEY=
# RESEND_FROM_EMAIL="OctoAgentFlow <no-reply@example.com>"

# Runtime secrets for non-local deployments.
# JWT_SECRET=
# X_TRENDS_BEARER_TOKEN=
# X_BEARER_TOKEN=
